<h1>Listing levels</h1>

<table>
  <thead>
    <tr>
      <th>Coin freq</th>
      <th>Mushroom freq</th>
      <th>Turtle freq</th>
      <th>Name</th>
      <th>Description</th>
      <th>Coin threshold</th>
      <th>Image</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @levels.each do |level| %>
      <tr>
        <td><%= level.coin_freq %></td>
        <td><%= level.mushroom_freq %></td>
        <td><%= level.turtle_freq %></td>
        <td><%= level.name %></td>
        <td><%= level.description %></td>
        <td><%= level.coin_threshold %></td>
        <td><%= level.image %></td>
        <td><%= link_to 'Show', level %></td>
        <td><%= link_to 'Edit', edit_level_path(level) %></td>
        <td><%= link_to 'Destroy', level, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Level', new_level_path %>
